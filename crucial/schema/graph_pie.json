{
  "name": "graph_pie",
  "description": "Draw a proportional pie chart using Crucial theme styles.",
  "parameters": {
    "type": "object",
    "properties": {
      "canvas_id": {
        "type": "string",
        "description": "Target canvas identifier."
      },
      "labels": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Labels for each pie slice. Must align with values."
      },
      "values": {
        "type": "array",
        "items": { "type": "number" },
        "description": "Values for each pie slice. Determines slice proportion."
      },
      "color": {
        "type": "string",
        "pattern": "^#(?:[0-9a-fA-F]{3}){1,2}$",
        "description": "Hex base color for shading (e.g., #ff6600)."
      },
      "theme": {
        "type": "string",
        "enum": ["light", "dark"],
        "default": "dark",
        "description": "Theme mode for background and label contrast."
      },
      "title": {
        "type": "string",
        "description": "Optional title text to show above chart."
      },
      "transparent": {
        "type": "boolean",
        "default": false,
        "description": "Whether the background is transparent or theme-colored."
      }
    },
    "required": ["canvas_id", "labels", "values", "color"]
  }
}

