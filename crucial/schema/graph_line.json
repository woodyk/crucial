{
  "name": "graph_line",
  "description": "Render a line graph from coordinate pairs using Crucial aesthetics.",
  "parameters": {
    "type": "object",
    "properties": {
      "canvas_id": {
        "type": "string",
        "description": "Canvas to render to"
      },
      "x_values": {
        "type": "array",
        "items": { "type": "number" },
        "description": "List of x values for the line"
      },
      "y_values": {
        "type": "array",
        "items": { "type": "number" },
        "description": "List of y values for the line"
      },
      "color": {
        "type": "string",
        "pattern": "^#(?:[0-9a-fA-F]{3}){1,2}$",
        "description": "Hex color code for the line (e.g. '#00ffcc')"
      },
      "title": {
        "type": "string",
        "description": "Optional chart title"
      },
      "theme": {
        "type": "string",
        "enum": ["light", "dark"],
        "default": "dark",
        "description": "Visual theme"
      },
      "transparent": {
        "type": "boolean",
        "default": false,
        "description": "Render on transparent background"
      },
      "x_label": {
        "type": "string",
        "description": "Label for x-axis"
      },
      "y_label": {
        "type": "string",
        "description": "Label for y-axis"
      }
    },
    "required": [
      "canvas_id",
      "x_values",
      "y_values",
      "color"
    ]
  }
}

