{
  "name": "graph_donut",
  "description": "Draw a Crucial-styled donut chart with central highlight label.",
  "parameters": {
    "type": "object",
    "properties": {
      "canvas_id": {
        "type": "string",
        "description": "Target canvas identifier."
      },
      "labels": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Labels for each donut slice. Must align with values."
      },
      "values": {
        "type": "array",
        "items": { "type": "number" },
        "description": "Values for each slice. Determines angular proportion."
      },
      "color": {
        "type": "string",
        "pattern": "^#(?:[0-9a-fA-F]{3}){1,2}$",
        "description": "Base hex color for shading (e.g., #3399ff)."
      },
      "theme": {
        "type": "string",
        "enum": ["light", "dark"],
        "default": "dark",
        "description": "Theme context for background and text color."
      },
      "title": {
        "type": "string",
        "description": "Optional title for the chart header."
      },
      "transparent": {
        "type": "boolean",
        "default": false,
        "description": "True to omit background fill, use alpha transparency."
      }
    },
    "required": ["canvas_id", "labels", "values", "color"]
  }
}

